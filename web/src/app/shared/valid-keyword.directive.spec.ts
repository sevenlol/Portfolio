import { ValidKeywordDirective } from './valid-keyword.directive';

describe('ValidKeywordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidKeywordDirective();
    expect(directive).toBeTruthy();
  });
});
